Build中选择全部的场景，然后选择PC平台，选择x86_64架构，压缩方法为默认，便可生成游戏

Select all the scenes in Build, then select the PC platform, select the x86_64 architecture, the compression method is the default, and the game can be generated